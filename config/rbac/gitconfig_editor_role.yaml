# permissions for end users to edit gitconfigs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: k8sversioner
    app.kubernetes.io/managed-by: kustomize
  name: gitconfig-editor-role
rules:
- apiGroups:
  - versioning.k8sversioner.app
  resources:
  - gitconfigs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - versioning.k8sversioner.app
  resources:
  - gitconfigs/status
  verbs:
  - get
