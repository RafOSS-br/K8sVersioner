# permissions for end users to view configs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: k8sversioner
    app.kubernetes.io/managed-by: kustomize
  name: config-viewer-role
rules:
- apiGroups:
  - versioning.k8sversioner.app
  resources:
  - configs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - versioning.k8sversioner.app
  resources:
  - configs/status
  verbs:
  - get
